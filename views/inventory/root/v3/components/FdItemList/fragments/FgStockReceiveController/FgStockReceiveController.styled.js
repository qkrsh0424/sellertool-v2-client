import styled from 'styled-components';

export const St = {
    Container: styled.div`
        button{
            background: var(--grayButtonColor);
            height: 32px;
            margin:0;
            padding: 0 16px;
            font-size: 14px;
            font-weight: 600;
            color: #000;
            border: none;
            border-radius: 8px;
        }
    `,
}