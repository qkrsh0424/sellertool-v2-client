import styled from 'styled-components';

export const St = {
    Container: styled.div`
        overflow: hidden;
        background: var(--defaultBackground);
        margin-bottom: 200px;

        padding: 20px;
    `
}